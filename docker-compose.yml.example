volumes:
    data:

services:
    postgres:
        image: postgres:latest
        environment:
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
        volumes:
            - data:/var/lib/postgresql
    web:
        build: .
        depends_on:
            - postgres
        ports:
            - 8080:8080